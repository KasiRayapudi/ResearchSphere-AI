# Shell scripts and Docker-related files must use LF, or the shebang breaks
# inside Linux containers when the repo is checked out on Windows.
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
Dockerfile* text eol=lf
*.conf text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
