apiVersion: 1

datasources:
  - name: Prometheus
    # The dashboards reference this uid through their DS_PROMETHEUS variable.
    uid: researchsphere-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s
      httpMethod: POST
