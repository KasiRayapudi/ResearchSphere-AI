fastapi==0.115.0
uvicorn[standard]==0.30.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
# Required by pydantic.EmailStr, used in app/api/v1/auth.py
email-validator==2.2.0
python-dotenv==1.0.1
qdrant-client==1.11.3
sentence-transformers==3.1.1
google-generativeai==0.8.2
langchain==0.3.1
langchain-google-genai==2.0.1
langchain-community==0.3.1
langgraph==0.2.28
PyMuPDF==1.24.11
python-docx==1.1.2
httpx==0.27.2
aiofiles==24.1.0
PyPDF2==3.0.1
tiktoken==0.8.0
numpy==1.26.4
redis==5.1.1
celery==5.4.0

# Object storage. Imported lazily by app/services/storage/s3.py, but declared
# here because STORAGE_PROVIDER=s3 (or minio) is the supported production
# configuration and the image must be able to serve it.
boto3==1.35.36

# Observability. Both are imported by app/core/metrics.py, which main.py
# imports at startup, so they are runtime dependencies rather than tooling:
# prometheus_client is a hard top-level import and the application cannot
# start without it, and psutil backs the disk/memory checks that /api/health
# and /api/ready report on.
prometheus-client==0.21.1
psutil==6.1.1
pytest==8.3.3
pytest-asyncio==0.24.0
